import {View, Text} from 'react-native';
import React from 'react';
import AppNavigator from './AppNavigator';

const MainContainer = () => {
  // app navigator is called here as this is the initial point for application
  return <AppNavigator />;
};

export default MainContainer;
